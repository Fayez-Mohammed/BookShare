@model Book_Sphere.ViewModel.AddRoleForExisingUserViewModel

@{
    ViewData["Title"] = "Assign Role to User";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-lg border-0 rounded-4">
                <!-- Card Header -->
                <div class="card-header text-white text-center rounded-top-4"
                     style="background: linear-gradient(135deg, #198754, #0f5132);">
                    <h3 class="mb-0">
                        <i class="bi bi-person-check-fill me-2"></i> Assign Role to User
                    </h3>
                </div>

                <!-- Card Body -->
                <div class="card-body p-4">
                   @*  <form asp-action="AddRoleToExsitingUser">
                        <div asp-validation-summary="All" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="Email" class="form-label fw-bold"></label>
                            <input asp-for="Email" class="form-control rounded-pill shadow-sm" placeholder="Enter user email..." />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="RoleName" class="form-label fw-bold"></label>
                            <select asp-for="RoleName" class="form-select rounded-pill shadow-sm" asp-items="Model.RolesList">
                                <option value="">-- Select Role --</option>
                            </select>
                            <span asp-validation-for="RoleName" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="RoleName" class="form-label fw-bold"></label>
                            <select asp-for="RoleName" class="form-select rounded-pill shadow-sm" asp-items="@(new SelectList(ViewBag.Allrols, "Name", "Name"))">
                                <option value="0">-- Select Role --</option>
                            </select>
                            <span asp-validation-for="RoleName" class="text-danger"></span>
                        </div>



                        <div class="d-flex justify-content-center gap-3">
                            <button type="submit" class="btn btn-success btn-lg rounded-pill shadow">
                                <i class="bi bi-plus-circle me-1"></i> Assign Role
                            </button>

                            <a asp-action="Role" class="btn btn-secondary btn-lg rounded-pill shadow">
                                <i class="bi bi-arrow-left-circle me-1"></i> Back
                            </a>
                        </div>
                    </form> *@


                    <form asp-action="RoleToExsitingUser" method="post">
                        <div asp-validation-summary="All" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="Email" class="form-label fw-bold"></label>
                            <input asp-for="Email" class="form-control rounded-pill shadow-sm" placeholder="Enter user email..." />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="RoleName" class="form-label fw-bold"></label>
                            <select asp-for="RoleName" class="form-select rounded-pill shadow-sm" asp-items="Model.RolesList">
                                <option value="">-- Select Role --</option>
                            </select>
                            <span asp-validation-for="RoleName" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-center gap-3">
                            <button type="submit" class="btn btn-success btn-lg rounded-pill shadow">
                                <i class="bi bi-plus-circle me-1"></i> Assign Role
                            </button>
                            <a asp-action="Role" class="btn btn-secondary btn-lg rounded-pill shadow">
                                <i class="bi bi-arrow-left-circle me-1"></i> Back
                            </a>
                        </div>
                    </form>



                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
